<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<SolidColorBrush x:Key="MainBgBrush" Color="#333333"/>
	<SolidColorBrush x:Key="ActiveTabBrush" Color="#D9E381"/>
	<SolidColorBrush x:Key="IikoBlueBrush" Color="#4D6E9E"/>
	<SolidColorBrush x:Key="DarkBgBrush" Color="#1D1D1D"/>
	<SolidColorBrush x:Key="PromoActiveBrush" Color="#D1D6A7"/>

	<Style x:Key="PromoTextBoxStyle" TargetType="TextBox">
		<Setter Property="Foreground" Value="Black"/>
		<Setter Property="BorderThickness" Value="0"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Padding" Value="10,0,35,0"/>
		<Setter Property="Height" Value="50"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="TextBox">
					<Grid>
						<Border x:Name="BackgroundBorder" Background="White" CornerRadius="2"/>
						<ScrollViewer x:Name="PART_ContentHost" Margin="5,0,35,0" VerticalAlignment="Center"/>
						<Button x:Name="ClearButton" Content="✕" HorizontalAlignment="Right" Width="35"
                                Background="Transparent" BorderThickness="0" Visibility="Collapsed"
                                Command="{Binding DataContext.ClearPromoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Foreground="#777" FontSize="18" FontWeight="Bold" Cursor="Hand"/>
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="Text" Value="">
							<Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed"/>
							<Setter TargetName="BackgroundBorder" Property="Background" Value="White"/>
						</Trigger>
						<DataTrigger Binding="{Binding Text.Length, RelativeSource={RelativeSource Self}}" Value="0">
							<Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed"/>
							<Setter TargetName="BackgroundBorder" Property="Background" Value="White"/>
						</DataTrigger>
						<DataTrigger Binding="{Binding Text.IsEmpty, RelativeSource={RelativeSource Self}}" Value="False">
							<Setter TargetName="ClearButton" Property="Visibility" Value="Visible"/>
							<Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource PromoActiveBrush}"/>
						</DataTrigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="IikoBottomButtonStyle" TargetType="Button">
		<Setter Property="Background" Value="{DynamicResource DarkBgBrush}"/>
		<Setter Property="Foreground" Value="White"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Button">
					<Border Background="{TemplateBinding Background}" BorderThickness="0.5" BorderBrush="#444">
						<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>